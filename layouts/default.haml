!!! 5
%html
  = render 'head'
  %body
    = render 'header'
    .main
      - if @item[:header_img]
        .row
          .twelve.columns
            %img{src:@item[:header_img]}
      = yield
    = render 'footer'

