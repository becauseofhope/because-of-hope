!!! 5
%html
  = render 'head'
  %body
    = render 'fbjsdk'
    = render 'header'
    .main
      - if @item[:header_img]
        .row
          .twelve.columns.banner
            %img{src:@item[:header_img]}
            %hr
      - if @item[:sidebar] == false
        = yield
      - else
        .row
          .eight.columns
            = yield
          .four.columns
            = render 'sidebar'
    = render 'footer'

